export type NavItem = {
  label: string;
  to: string;
};

export type StatItem = {
  value: string;
  label: string;
};

export type SpeakingTopic = {
  title: string;
  description: string;
  audience: string;
};

export type ServiceOffer = {
  title: string;
  description: string;
  outcomes: string[];
};

